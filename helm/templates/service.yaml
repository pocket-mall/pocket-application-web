apiVersion: v1
kind: Service
metadata:
  name: {{ include "pocket-m.fullname" . }}
  labels:
    {{- include "pocket-m.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pocket-m.selectorLabels" . | nindent 4 }}
